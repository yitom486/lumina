---
description: 错误处理全链路 — Agent 必读
alwaysApply: true
---

# Errors

## 产品目标（先读这个）

**对用户隐藏实现。** 不是「翻译成中文」本身，而是：

- UI **只**展示业务语义（能做什么 / 出了什么问题 / 建议下一步）
- **禁止**把底层失败（工具名、stderr、serde、JSON-RPC、路径、exit code、FFI）展示给用户
- 底层信息只进 `details` + `tracing`，供开发排查

`message` 用中文，是因为产品语言是中文；**即便英文产品，也不能把底层原文当 UI 文案。**

## 形状（前后端一致）

```json
{ "code": "PascalCaseEnum", "message": "给用户看的业务中文", "details": "可选技术细节（勿展示）" }
```

| 字段 | 用途 | UI |
|------|------|-----|
| `code` | 稳定枚举；前端兜底 / 分支 | 可用来选文案，不要直接展示枚举名给用户 |
| `message` | 固定业务语义 | **唯一**默认展示内容 |
| `details` | 工具原文、路径、serde… | **默认永不渲染**；仅日志或显式「开发者调试」开关 |

## 分层（硬性）

```
底层失败（spawn / stderr / serde / lock / join）
  → 域构造函数 → 固定业务 message
  → details = 技术原文
  → tracing::warn! / error!
  → UI：formatPlayerError / errorMessage → 只取 message（或 code 兜底）
```

**禁止**在 `message` 或任何用户可见字符串中出现：`ffprobe` / `ffmpeg` / `whisper-cli` / `libmpv` / JSON / serde / 堆栈 / 原始 stderr。
**允许**：校验类具体业务句（`bad_request("提问内容不能为空")`、`invalid("笔记内容不能为空")`）。
**禁止**：React 组件里 `error.details`、`title={details}`、「详情：…」一类展示（见历史踩坑：`VideoSurface`）。

域构造函数：每个故障类 `code` 固定 `message`；调用方只传 `details: Option<&str>`。

## 链路

```
Domain (Player / Media / Subtitle / Asr / Acp / Note)
  → *Error { code, message, details? }
  → Tauri Command / Channel
  → formatPlayerError / errorMessage（忽略 details）
  → UI；tracing 记完整错误
```

## 域错误码与固定 message（参考）

| 域 | code | 典型 message |
|----|------|----------------|
| Player | LoadError | 无法打开该媒体文件 |
| Player | PlaybackError | 播放操作失败，请重试 |
| Player | InvalidState | 当前为「…」状态，无法执行「…」 |
| Media | ProbeFailed | 无法读取该视频的媒体信息 |
| Media | ProbeNotFound | 媒体分析组件未就绪 |
| Subtitle | ExtractFailed | 无法提取字幕 |
| Subtitle | ParseFailed | 无法解析字幕 |
| Subtitle | ToolNotFound | 字幕工具未就绪 |
| Asr | NotConfigured | 未配置语音转写（可选） |
| Asr | TranscribeFailed | 语音转写失败 |
| Acp | NotConfigured | 未配置 AI Agent（可选） |
| Acp | SpawnFailed | 无法启动 AI Agent |
| Acp | ProtocolError | 与 Agent 通信失败（校验用 `bad_request`） |
| Note | IoError | 笔记读写失败 |
| * | InternalError | 内部错误，请重试 |

安装路径、CLI 名称 → `details` 或**设置/帮助**文案，不要塞进失败时的主 `message`。

## 硬性规则

1. 业务路径禁止 `unwrap` / `expect`
2. 可恢复失败勿 `fail()` 成整机会话 `Error`
3. 重 I/O（ffmpeg / ffprobe / whisper / ACP spawn）必须 `async + spawn_blocking`
4. React 禁止猜测播放状态；错误以 Rust / invoke 为准
5. 构造函数：`message` 中文且无工具名；单测断言 CJK + 断言工具名不在 `message`
6. 改文案 / code → 同步域单测、`format.ts`、`format.test.ts`
7. **前端禁止渲染 `details`**（除非独立 debug 模式且默认关闭）

## 测试期望

- `cargo test --lib`
- `bun run test`（`formatPlayerError`：有 CJK message 时绝不回落到英文/工具原文）
