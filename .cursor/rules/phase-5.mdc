---
description: Phase 5 — ACP Client 可插拔；默认 Codex；非打开视频必经路径
alwaysApply: true
---

# Phase 5

Lumina 只做 **ACP Client**；Agent **可选、按需、可插拔**。

## 分层

- **ACP**：产品协议边界（stdio JSON-RPC）
- **codex-acp**：默认适配器（多为 TS/Node，可单文件）；内部再启 **Codex App Server**
- **不要**把 App Server 私有协议当主路径（会锁死 Codex）
- Codex 模型侧：**Responses API only**（非任意 Chat Completions URL）

## 规则

- 禁止：启动预连 Agent、Open 时自动开会话、把 Codex / 强制用户安装 bun 打进「不装就不能播」的路径
- 允许：用户发起后按 **AgentProfile** spawn；进度经 Channel
- 默认 profile：`codex`；可加 `claude` / `custom`（换 harness = 换进程，不是塞进 App Server）
- 未配置 → `NotConfigured`；播放、字幕、笔记仍可用
- React 不知子进程；Rust 管 spawn 与生命周期
- **bun 仅开发构建**；终端用户播放不需要 bun；ACP 可选（适配器可自带 Codex）
