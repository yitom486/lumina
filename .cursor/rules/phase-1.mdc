---
description: Phase 1 已完成；保留验收记忆，不再阻塞 Phase 2
alwaysApply: false
---

# Phase 1（已完成）

Native Playback Spike 已验收：`libmpv` + wid 嵌入 + 基本控制。

历史禁区已解除中的「FFmpeg extraction」——Phase 2 允许 **只读 ffprobe 探测**，仍禁止转码/抽帧流水线/ASR 等（见 `phase-2.mdc`）。
