{
  "name": "lumina-workspace",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": ["apps/desktop", "packages/contracts", "packages/query-keys", "packages/player-ui", "packages/transcript-ui", "packages/chat-ui", "packages/ui"],
  "devDependencies": {
    "oxlint": "^1.81.0"
  },
  "scripts": {
    "dev": "cd apps/desktop && bun run dev",
    "build": "cd apps/desktop && bun run build",
    "preview": "cd apps/desktop && bun run preview",
    "typecheck": "cd apps/desktop && bun run typecheck",
    "lint": "bun run check:contracts && cd apps/desktop && bun run lint",
    "check:contracts": "bun ./packages/contracts/scripts/check.mjs",
    "lint:ox": "oxlint apps/desktop/src packages/contracts/src packages/query-keys/src packages/player-ui/src packages/transcript-ui/src packages/chat-ui/src packages/ui/src",
    "lint:all": "bun run lint && bun run lint:ox",
    "check:rust": "cargo fmt --all --check && cargo check --workspace --all-targets && cargo test --workspace --lib && cargo clippy --workspace --all-targets -- -D warnings",
    "test": "cd apps/desktop && bun run test",
    "test:e2e:online": "cargo test --manifest-path Cargo.toml -p lumina-ytdl --lib public_youtube_resolves_without_cookies -- --ignored --nocapture",
    "test:e2e:deterministic": "node scripts/test-deterministic-e2e.mjs",
    "test:e2e:acp-mcp-priority": "node scripts/test-acp-mcp-tool-priority.mjs",
    "test:unit": "cd apps/desktop && bun run test:unit",
    "test:ui": "cd apps/desktop && bun run test:ui",
    "tauri": "node scripts/tauri-dev.mjs"
  }
}
