# Optional Codex ACP binaries

Place `codex-acp.exe` (and optionally `codex.exe`) here for local discovery,
or install them on PATH. Lumina does **not** bundle these.

Never required for playback, notes, or subtitles.
