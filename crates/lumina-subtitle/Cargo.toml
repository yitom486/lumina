[package]
name = "lumina-subtitle"
version = "0.4.0"
description = "Subtitle discovery, extraction, parsing, and export for Lumina"
edition = "2021"

[dependencies]
lumina-media.workspace = true
serde = { version = "1", features = ["derive"] }
tracing = "0.1"
