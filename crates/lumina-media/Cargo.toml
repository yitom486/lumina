[package]
name = "lumina-media"
version = "0.4.0"
description = "Media inspection and local media-tool integration for Lumina"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
which = "7"
