[package]
name = "lumina-core"
version = "0.4.0"
description = "Stable, implementation-free value objects for Lumina"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
