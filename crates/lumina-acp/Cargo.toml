[package]
name = "lumina-acp"
version = "0.3.0"
description = "Optional generic ACP client for Lumina"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
