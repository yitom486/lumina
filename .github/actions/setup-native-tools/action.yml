name: Setup native tools
description: >-
  Install libmpv and ffmpeg into apps/desktop/src-tauri/native (shared by CI validate,
  CI bundle and release). Scripts are the canonical copies; workflows must
  `uses:` this action instead of pasting them. VERSION files stay the single
  pinning point.

inputs:
  platform:
    description: "Runner platform: windows | macos | linux"
    required: true

runs:
  using: composite
  steps:
    - name: Install libmpv (Windows)
      if: inputs.platform == 'windows'
      shell: pwsh
      run: |
        $mpvDir = 'apps/desktop/src-tauri/native/mpv'
        $metadata = @{}
        Get-Content "$mpvDir/VERSION" | ForEach-Object {
          if ($_ -match '^([^=]+)=(.*)$') { $metadata[$matches[1]] = $matches[2] }
        }
        if (-not (Test-Path "$mpvDir/libmpv-2.dll") -or -not (Test-Path "$mpvDir/mpv.lib")) {
          $archive = Join-Path $env:RUNNER_TEMP $metadata['archive']
          $extractDir = Join-Path $env:RUNNER_TEMP 'lumina-mpv-dev'
          curl.exe -L --fail --retry 3 -o $archive $metadata['url']
          New-Item -ItemType Directory -Force -Path $extractDir | Out-Null
          tar.exe -xf $archive -C $extractDir
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $dll = Get-ChildItem -Path $extractDir -Recurse -Filter 'libmpv-2.dll' -File | Select-Object -First 1
          if ($null -eq $dll) {
            throw 'The libmpv archive did not contain libmpv-2.dll.'
          }
          Copy-Item $dll.FullName "$mpvDir/libmpv-2.dll" -Force
          $exportNames = & dumpbin.exe /exports "$mpvDir/libmpv-2.dll" |
            ForEach-Object {
              if ($_ -match '^\s*\d+\s+[0-9A-F]+\s+[0-9A-F]+\s+(\S+)$') { $matches[1] }
            }
          if ($LASTEXITCODE -ne 0 -or $exportNames.Count -eq 0) {
            throw 'Could not read libmpv exports to create an MSVC import library.'
          }
          @('LIBRARY libmpv-2.dll', 'EXPORTS') + ($exportNames | ForEach-Object { "  $_" }) |
            Set-Content "$mpvDir/mpv.def" -Encoding ascii
          & lib.exe "/def:$mpvDir/mpv.def" /machine:x64 "/out:$mpvDir/mpv.lib"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
        }
        $runtimeDir = "$mpvDir/runtime"
        New-Item -ItemType Directory -Force -Path $runtimeDir | Out-Null
        Copy-Item "$mpvDir/libmpv-2.dll" "$runtimeDir/libmpv-2.dll" -Force

    - name: Install ffmpeg (Windows)
      if: inputs.platform == 'windows'
      shell: pwsh
      run: |
        $ffmpegDir = 'apps/desktop/src-tauri/native/ffmpeg'
        $metadata = @{}
        Get-Content "$ffmpegDir/VERSION" | ForEach-Object {
          if ($_ -match '^([^=]+)=(.*)$') { $metadata[$matches[1]] = $matches[2] }
        }
        if (-not (Test-Path "$ffmpegDir/ffprobe.exe") -or -not (Test-Path "$ffmpegDir/ffmpeg.exe")) {
          $archive = Join-Path $env:RUNNER_TEMP 'ffmpeg-essentials.zip'
          $extractDir = Join-Path $env:RUNNER_TEMP 'lumina-ffmpeg-extract'
          curl.exe -L --fail --retry 3 -o $archive $metadata['url']
          New-Item -ItemType Directory -Force -Path $extractDir | Out-Null
          tar.exe -xf $archive -C $extractDir
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $ffprobe = Get-ChildItem -Path $extractDir -Recurse -Filter 'ffprobe.exe' -File | Select-Object -First 1
          $ffmpeg = Get-ChildItem -Path $extractDir -Recurse -Filter 'ffmpeg.exe' -File | Select-Object -First 1
          if ($null -eq $ffprobe -or $null -eq $ffmpeg) {
            throw 'The ffmpeg archive did not contain ffprobe.exe / ffmpeg.exe.'
          }
          Copy-Item $ffprobe.FullName "$ffmpegDir/ffprobe.exe" -Force
          Copy-Item $ffmpeg.FullName "$ffmpegDir/ffmpeg.exe" -Force
        }
        Get-ChildItem $ffmpegDir -Filter '*.exe' | ForEach-Object { "{0} {1}" -f $_.Name, $_.Length }

    - name: Install FFmpeg (macOS/Linux)
      if: inputs.platform == 'macos' || inputs.platform == 'linux'
      shell: bash
      run: |
        set -euo pipefail
        ffmpeg_dir='apps/desktop/src-tauri/native/ffmpeg'
        version_file="$ffmpeg_dir/VERSION"
        unix_version=$(sed -n 's/^unix_version=//p' "$version_file")
        url_template=$(sed -n 's/^unix_url=//p' "$version_file")
        test -n "$unix_version"
        test -n "$url_template"

        case "$(uname -m)" in
          x86_64|amd64) arch='amd64' ;;
          arm64|aarch64) arch='arm64' ;;
          *) echo "Unsupported runner architecture: $(uname -m)" >&2; exit 1 ;;
        esac

        platform='${{ inputs.platform }}'
        tmp_dir="${RUNNER_TEMP:-/tmp}/lumina-ffmpeg-${platform}-${arch}"
        rm -rf "$tmp_dir"
        mkdir -p "$tmp_dir" "$ffmpeg_dir"

        for tool in ffmpeg ffprobe; do
          url="${url_template//\{platform\}/$platform}"
          url="${url//\{arch\}/$arch}"
          url="${url//\{tool\}/$tool}"
          archive="$tmp_dir/$tool.zip"
          extract_dir="$tmp_dir/$tool"
          mkdir -p "$extract_dir"
          curl --fail --location --retry 3 --retry-delay 2 --silent --show-error \
            "$url" --output "$archive"
          unzip -q -o "$archive" -d "$extract_dir"
          binary=$(find "$extract_dir" -type f -name "$tool" -print -quit)
          if [ -z "$binary" ]; then
            echo "The FFmpeg archive did not contain $tool for $platform/$arch." >&2
            exit 1
          fi
          install -m 0755 "$binary" "$ffmpeg_dir/$tool"
        done

        actual_version=$("$ffmpeg_dir/ffmpeg" -version | sed -n '1s/^ffmpeg version \([^ ]*\).*/\1/p')
        # Prefix match: builders append suffixes (e.g. `-https://www.martin-riedl.de`)
        # that must not fail the gate as long as the pinned version leads.
        case "$actual_version" in
          "$unix_version"*) ;;
          *)
            echo "Expected FFmpeg $unix_version, got $actual_version." >&2
            exit 1
            ;;
        esac
        "$ffmpeg_dir/ffprobe" -version >/dev/null
        ls -l "$ffmpeg_dir/ffmpeg" "$ffmpeg_dir/ffprobe"

    - name: Install libmpv (macOS)
      if: inputs.platform == 'macos'
      shell: bash
      run: |
        brew install mpv pkg-config
        mkdir -p apps/desktop/src-tauri/native/mpv/runtime
        # brew 的 libmpv*.dylib 多为指向 Cellar 的 symlink；tauri_build 打包资源时
        # 跟随 symlink 可能 Permission denied。解引用成可读写实体文件。
        python3 - <<'PY'
        import os
        import shutil
        import subprocess
        from pathlib import Path

        prefix = subprocess.check_output(["brew", "--prefix", "mpv"], text=True).strip()
        src = Path(prefix) / "lib"
        dst = Path("apps/desktop/src-tauri/native/mpv/runtime")
        dst.mkdir(parents=True, exist_ok=True)
        staged = False
        for path in sorted(src.glob("libmpv*.dylib")):
            target = path.resolve()
            out = dst / path.name
            if out.exists() or out.is_symlink():
                out.unlink()
            shutil.copy2(target, out)
            os.chmod(out, 0o755)
            print(f"staged {out} <- {target} ({out.stat().st_size} bytes)")
            staged = True
        if not staged:
            raise SystemExit(f"no libmpv*.dylib under {src}")
        PY
        ls -la apps/desktop/src-tauri/native/mpv/runtime/

    - name: Install build deps (Linux)
      if: inputs.platform == 'linux'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libwebkit2gtk-4.1-dev \
          libayatana-appindicator3-dev \
          librsvg2-dev \
          libssl-dev \
          libmpv-dev \
          libx11-dev \
          patchelf
        mkdir -p apps/desktop/src-tauri/native/mpv/runtime
        shopt -s nullglob
        staged=0
        for lib in /usr/lib/x86_64-linux-gnu/libmpv.so*; do
          # -L 解引用，避免把指向系统路径的 symlink 交给 tauri resources
          cp -L "$lib" apps/desktop/src-tauri/native/mpv/runtime/
          chmod a+rX "apps/desktop/src-tauri/native/mpv/runtime/$(basename "$lib")"
          staged=1
        done
        if [ "$staged" -ne 1 ]; then
          echo "no libmpv.so* found under /usr/lib/x86_64-linux-gnu" >&2
          exit 1
        fi
        ls -la apps/desktop/src-tauri/native/mpv/runtime/
