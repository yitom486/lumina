import { TranscriptPanel } from "./TranscriptPanel";

/** Settings-owned subtitle acquisition, ASR, translation, and proofreading. */
export function SubtitleWorkshopPanel() {
  return <TranscriptPanel view="workshop" />;
}
