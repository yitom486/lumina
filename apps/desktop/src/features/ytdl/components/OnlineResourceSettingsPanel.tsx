import { OnlineSourcePanel } from "./OnlineSourcePanel";

/** Settings-owned URL, download, resolver, and cookie operations. */
export function OnlineResourceSettingsPanel() {
  return <OnlineSourcePanel view="settings" />;
}
