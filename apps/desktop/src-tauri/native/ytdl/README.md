# yt-dlp（可选 · 按需）

在线视频解析组件。**不进默认安装包**；用户需要打开链接时再下载到：

- Windows：`%APPDATA%\lumina\yt-dlp\yt-dlp.exe`

开发期也可手动放到本目录（`yt-dlp.exe`），优先于应用数据目录。

应用内下载会固定到 GitHub Release 的具体版本；当在线视频站点规则变化时，可在设置里重新下载或更新。
